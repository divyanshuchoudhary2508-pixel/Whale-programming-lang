// std::string
// Fast C-FFI string manipulation functions

extern fn strlen(s: string) -> int;
extern fn strcmp(s1: string, s2: string) -> int;

pub fn len(s: string) -> int {
    return strlen(s);
}

pub fn compare(s1: string, s2: string) -> int {
    return strcmp(s1, s2);
}
